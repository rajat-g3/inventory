Technology Used:
1. PHP
2. HTML
3. CSS/Bootstrap
4. Javascript/jQuery


Create a DB named `inventorydb` and run the SQL commands from `db.sql` file inside `database` folder and run http://localost/inventory inside browser to run the application.